# Host the VM can query (use 127.0.0.1 if ports are published to host)
SERVER_HOST=127.0.0.1
# Steam A2S query port (usually game port+1; default Valheim => 2457)
SERVER_QUERY_PORT=2457

# Minutes of zero players before shutdown
# No need to reload when changing this, new value takes effect next timer run.
IDLE_MINUTES=60
BOOT_GRACE_MINUTES=3 # Prevent immediate shutdown if IDLE_MINUTES too low

# A2S query robustness
A2S_TIMEOUT_SEC=2
A2S_RETRIES=3
A2S_RETRY_DELAY_SEC=3

# State file (do NOT put this inside the Git repo)
STATE_FILE=/var/lib/server-idle/last_active.txt

# Container to stop gracefully before poweroff
CONTAINER_NAME=valheim-server